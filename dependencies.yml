collectors:

- type: js-npm
  path: www/mdl
  actors:
  # pull requests for all major, minor, and patch updates
  - type: js-npm
    versions: "Y.Y.Y"

- type: php-composer
  path: /
  actors:
  # pull requests for all major, minor, and patch updates
  - type: php-composer
    versions: "Y.Y.Y"

- type: js-npm
  path: /
  actors:
  # pull requests for all major, minor, and patch updates
  - type: js-npm
    versions: "Y.Y.Y"
